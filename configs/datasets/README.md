# Dataset Configurations

CIFAR-10 will be the first dataset supported by the new experimental pipeline.

Future configuration files must be machine-readable and portable. They must not contain invented split sizes or paths tied to a contributor's local machine.
